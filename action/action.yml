name: SimpiCI runner
description: Run the repository-owned SimpiCI jobs for this exact revision

runs:
  using: composite
  steps:
    - name: Run SimpiCI jobs
      shell: bash
      run: "${{ github.action_path }}/run.sh"
